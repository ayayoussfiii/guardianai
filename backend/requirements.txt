# ─── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ─── Validation ───────────────────────────────────────────────────────────────
pydantic>=2.7.0

# ─── Middleware (stdlib uniquement, pas de dépendances externes) ───────────────
# scanner.py  → re, unicodedata, threading, enum, dataclasses, typing
# filter.py   → html, re, unicodedata, threading, dataclasses, typing
# detector.py → re, threading, time, uuid, dataclasses, logging, typing

# ─── Logging (optionnel selon le handler choisi) ──────────────────────────────
# python-json-logger>=2.0.7   # si logs structurés JSON

# ─── Dev / test ───────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # client de test pour FastAPI
